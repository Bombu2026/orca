---
description: Run all self-check assertions
---
Run `bun scripts/self-check.ts` and show the full output. If any assertion fails, diagnose and fix immediately — never leave the skill in a broken state. After fixing, re-run to confirm all assertions pass (N/N green — the script prints the total).
