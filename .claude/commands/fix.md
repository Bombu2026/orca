---
description: Fix all failing self-check assertions until clean
---
Run `bun scripts/self-check.ts` and fix all failures iteratively until every assertion passes (N/N green). Show a summary of what was fixed. Do not introduce new features — only fix broken assertions.
