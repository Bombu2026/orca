---
description: Create a pull request for the current branch
---
Create a PR for the current branch against {{DEFAULT_BRANCH}}. Use `gh pr create` with a concise title and structured body (Summary + Test Plan). Include all commits since branch divergence.
