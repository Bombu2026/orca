---
description: Create a conventional commit from staged changes
---
Analyze the staged diff (`git diff --cached`). Write a conventional commit message following the project's commit style. The message should be concise (1 line subject, optional body). Do NOT push. Wait for user confirmation before committing.
