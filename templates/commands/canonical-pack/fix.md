---
description: Fix all build/type errors until clean
---
Run `{{BUILD_COMMAND}}` and fix all errors iteratively until the command exits clean. Show a summary of what was fixed. Do not introduce new features — only fix existing errors.
