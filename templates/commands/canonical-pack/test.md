---
description: Run tests and fix failures until green
---
Run `{{TEST_COMMAND}}` and fix all failures iteratively until tests pass. Focus on fixing the test subject, not weakening the tests. Show a summary of what was fixed.
